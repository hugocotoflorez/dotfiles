# dotfiles

## nvim install script

Script in ./.scripts/ is not working yet

### manual installation

copy folder REPO/.config/nvim to local .config/nvim

install packer and install packages ( see archived hugocotoflorez/vim repo for more details )

remember that errors at first start must be ignored and overpassed pressing enter key
